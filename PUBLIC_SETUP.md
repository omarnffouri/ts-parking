# Local service configuration

This public snapshot intentionally omits Firebase configuration files and replaces embedded Google API keys with `REPLACE_WITH_GOOGLE_API_KEY`.

Before building, download configuration files for your own Firebase project and place them at `android/app/google-services.json` and `ios/Runner/GoogleService-Info.plist`. Keep those files untracked.
